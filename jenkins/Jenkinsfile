pipeline {
  agent any

   parameters([choice(choices:['bug fixw', 'feature', 'release'],
                description: 'escolha o tipo de build',
                name: 'build_type')])
  stages {
    stage('Example') {
      steps {
        echo 'Hello'
        echo "${params.build_type}"
      }
    }
  }
}